enum Failure { ASSERT DURATION COST }
record Input { text: String }
workflow Example(input: Input) -> Input fails Failure {
  capabilities {}
  limits {
    tool_calls <= 0
    model_calls <= 0
    duration <= 1s else DURATION
    duration <= 2s else DURATION
    cost <= 0 USD else COST
  }
  return input
}
